#include "p1.tb"
#include "p2.tb"
#include "user-interface.idef"
#include "editor-hive.idef"

tool user-environment is {}

process UE is
let
  UE : user-environment,
  Pid1 : int,
  Pid2: int
in
  create(P1, Pid1?)
  . create(P2, Pid2?)
  . rec-connect(UE?) 
  .
  (
    UI(UE)
  ||
    Hive(UE)
  )
endlet

toolbus(UE)
